## What this changes

<!-- Describe the change in terms of what a child sees or hears. -->

## Checklist

- [ ] Still a single self-contained `index.html` (no new deps, no external fetches)
- [ ] `npm run validate` passes
- [ ] `shellcheck build-deb.sh` passes (if the script changed)
- [ ] `./build-deb.sh` still produces a `.deb` (if packaging changed)
- [ ] Comments mapping behaviour to the original `gamine.c` are still accurate
- [ ] I license this contribution under GPL-3.0-or-later
